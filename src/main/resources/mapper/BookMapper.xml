<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlybl.mapper.BookMapper">
    <!--全查-->
    <select id="selectAllBook" resultType="Book">
        select * from book
    </select>
    <!--可选择条件查询-->
    <select id="selectBooksByConditions" resultType="Book">
        SELECT * FROM book WHERE (bookName like CONCAT('%',#{bookName},'%') OR CONCAT('%',#{bookName},'%')='')
                             AND (author like CONCAT('%',#{author},'%') OR CONCAT('%',#{author},'%')='')
                             AND (bookCover like CONCAT('%',#{bookCover},'%') OR CONCAT('%',#{bookCover},'%')='')
                             AND (bookStatus like CONCAT('%',#{bookStatus},'%') OR CONCAT('%',#{bookStatus},'%')='')
                             AND (press like CONCAT('%',#{press},'%') OR CONCAT('%',#{press},'%')='')
                             AND (bookClass like CONCAT('%',#{bookClass},'%') OR CONCAT('%',#{bookClass},'%')='');
    </select>
    <!--选择查询-->
    <select id="selectBookByConditions" resultType="Book">
        SELECT * FROM book WHERE (bookName like CONCAT('%',#{bookName},'%') OR CONCAT('%',#{bookName},'%')='')
                             AND (author like CONCAT('%',#{author},'%') OR CONCAT('%',#{author},'%')='')
                             AND (bookCover like CONCAT('%',#{bookCover},'%') OR CONCAT('%',#{bookCover},'%')='')
                             AND (bookStatus like CONCAT('%',#{bookStatus},'%') OR CONCAT('%',#{bookStatus},'%')='')
                             AND (press like CONCAT('%',#{press},'%') OR CONCAT('%',#{press},'%')='')
                             AND (bookClass like CONCAT('%',#{bookClass},'%') OR CONCAT('%',#{bookClass},'%')='');
    </select>
    <!--增加书籍-->
    <insert id="addBook">
        insert into book(bookCover,bookName,author,press,bookClass) values (#{bookCover},#{bookName},#{author},#{press},#{bookClass})
    </insert>
    <!--修改数据-->
    <update id="updateBook">
        update book set bookCover=#{bookCover},bookName=#{bookName},author=#{author},press=#{press},bookClass=#{bookClass}
        where id=#{id}
    </update>
    <!--删除数据-->
    <delete id="deleteBook">
        delete from book where id=#{id}
    </delete>
</mapper>